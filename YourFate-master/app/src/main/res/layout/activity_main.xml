<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_light"
    android:padding="16dp"
    tools:context=".MainActivity">

    <!-- Header -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/accent"
        android:padding="14dp"
        android:layout_alignParentTop="true"
        android:layout_marginBottom="12dp">

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="เซียมซี"
            android:textSize="22sp"
            android:textColor="@android:color/white"
            android:fontFamily="sans-serif-medium"/>

        <TextView
            android:id="@+id/tvSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="ขอคำทำนาย จากเซียมซีเสี่ยงทาย"
            android:textSize="13sp"
            android:textColor="@android:color/white"
            android:layout_marginTop="4dp"/>
    </LinearLayout>

    <!-- Main content area: left = jar, right = result card -->
    <LinearLayout
        android:id="@+id/content"
        android:layout_below="@id/header"
        android:layout_above="@id/footer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="12dp"
        android:orientation="horizontal">

        <!-- เสาเซียมซี / โหล (placeholder image) -->
        <FrameLayout
            android:id="@+id/jarArea"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:padding="12dp"
            android:foregroundGravity="center">

            <!-- ใส่รูปโหลเซียมซีที่นี่ (replace drawable) -->
            <ImageView
                android:id="@+id/ivJar"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_siamm_jar"
                android:contentDescription="โหลเซียมซี"
                android:adjustViewBounds="true"
                android:layout_gravity="center" />

            <!-- แท่งเซียมซีที่โผล่มา (เริ่มซ่อน) -->
            <TextView
                android:id="@+id/tvStick"
                android:layout_width="56dp"
                android:layout_height="200dp"
                android:layout_gravity="center"
                android:background="@drawable/stick_rounded"
                android:text="卜"
                android:textSize="36sp"
                android:textColor="@color/accent_dark"
                android:gravity="center"
                android:visibility="gone"/>
        </FrameLayout>

        <!-- ผลทำนาย -->
        <androidx.cardview.widget.CardView
            android:id="@+id/resultCard"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1.1"
            android:layout_marginStart="12dp"
            android:cardCornerRadius="12dp"
            android:cardElevation="6dp"
            android:padding="16dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvResultTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="คำทำนาย"
                    android:textSize="18sp"
                    android:textColor="@color/accent_dark"
                    android:fontFamily="sans-serif-medium"/>

                <TextView
                    android:id="@+id/tvResultNumber"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="—"
                    android:textSize="44sp"
                    android:textStyle="bold"
                    android:layout_marginTop="20dp"
                    android:textColor="@color.accent"/>

                <TextView
                    android:id="@+id/tvPrediction"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="กดปุ่มด้านล่างเพื่อสุ่มเซียมซี"
                    android:textSize="16sp"
                    android:layout_marginTop="16dp"
                    android:textColor="#444"/>

                <!-- พื้นที่รายละเอียดเพิ่มเติม (scrollable) -->
                <ScrollView
                    android:layout_width="match_parent"
                    android:layout_height="0dp"
                    android:layout_weight="1"
                    android:layout_marginTop="12dp">

                    <TextView
                        android:id="@+id/tvPredictionFull"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:text=""
                        android:textSize="14sp"
                        android:textColor="#333"/>
                </ScrollView>

            </LinearLayout>
        </androidx.cardview.widget.CardView>
    </LinearLayout>

    <!-- Footer: ปุ่มสุ่ม/เขย่า -->
    <LinearLayout
        android:id="@+id/footer"
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:paddingTop="8dp"
        android:paddingBottom="8dp">

        <Button
            android:id="@+id/btnDraw"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="56dp"
            android:text="ดึงเซียมซี"
            android:background="@drawable/btn_rounded"
            android:textColor="@android:color/white"
            android:layout_marginEnd="8dp"/>

        <Button
            android:id="@+id/btnShake"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="56dp"
            android:text="เขย่า (ทดสอบ)"
            android:background="@drawable/btn_outline"
            android:textColor="@color/accent_dark"/>
    </LinearLayout>
</RelativeLayout>
